// src/lib/cookies.ts
export const AUTH_COOKIE_NAME = 'cashflow_session';

export const AUTH_COOKIE_OPTIONS = {
  httpOnly: true,
  secure: process.env.NODE_ENV === 'production',
  sameSite: 'lax' as const,
  path: '/',
  maxAge: 60 * 60 * 24 * 7, 
};